<img class="position-absolute top-0 bottom-0 my-auto mx-0 @(HasCostume ? "fade-in-out" : "")" src="img/weapon/@(Weapon.FileName)_large.png" />

@code {
    [Parameter]
    [EditorRequired]
    public GachaWeapon Weapon { get; set; }

    [Parameter]
    public bool HasCostume{ get; set; }
}
